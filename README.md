# Week 4 Assignment - Grades, Eligibility & Smart Decisions

welcome.py - Contains a welcome() function that returns a greeting for a given name.

toolbox.py - Contains three functions: double(), is_pass(), and greet().

The greet() function was the hardest to write because it uses a default parameter and needs to combine the greeting and name correctly.# Week 4 Assignment - Grades, Eligibility & Smart Decisions

welcome.py - Contains a welcome() function that returns a greeting for a given name.

toolbox.py - Contains three functions: double(), is_pass(), and greet().

The greet() function was the hardest to write because it uses a default parameter and needs to combine the greeting and name correctly.